import { Product } from "../models";

export const getProducts = () /*: Promise<Product[]> */ => {
	/*const url = "http://localhost:7250/api/Products";
	return fetch(url)
		.then((res) => res.json())
		.then((data) => data);*/
};
